[project]
name = "acceptance-shard-app"
version = "0.1.0"
description = "Seed project for a Vera acceptance shard repo."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
