# Managed by tests/acceptance/conftest.py (_ensure_project_settings).
# Pins the documented product default so acceptance runs are deterministic.
# TestVeraLabelGating asserts that an unlabeled issue draws no bot activity;
# that is only true while Vera is not opted in to adopting unlabeled issues.
# See vera#5372.
auto_manage_issues: false
